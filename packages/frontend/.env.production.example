# frontend/.env.production - these variables are used by vite in development mode
# https://vitejs.dev/guide/env-and-mode

BACKEND_PROTOCOL="http"
BACKEND_IP_ADDRESS="backend" # docker compose backend service name, we let docker compose resolve the IP address for us.
BACKEND_PORT="80" # In production we use a reverse proxy / load balancer to handle https-to-http conversion and let the services run on port 80 for increased performance.

EXTERNAL_API_KEY="some-external-api-key"